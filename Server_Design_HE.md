# עיצוב שרת — Server Design למשחק מרובה משתתפים בקנה מידה גדול

## 1. סקירה כללית

מסמך זה מתאר את העיצוב הנדרש כדי לתמוך ב:
- 100,000,000 משתמשים רשומים
- 10,000,000 שחקנים פעילים בו-זמנית מכל העולם
- משחק בזמן אמת, כ-1 מהלך כל 2 שניות לשחקן פעיל
- משחקים קצרים (30–90 שניות כל אחד)

העיצוב מבוסס על הכיוון שקיבלנו מהריוורים (API Gateway / WS Gateway / Matchmaker / Game Allocator / Game Server Shards / NATS event bus / Redis / PostgreSQL / Kubernetes-K3s), מותאם למנוע המשחק וליישום הקיים שלנו.

עיקרון-על שממשיך מהקוד הקיים שלנו: **ה-client לא מחליט על חוקי המשחק, וגם לא ה-Gateway. ה-`GameEngine` שרץ בתוך Game Server Shard הוא ה-single source of truth היחיד למצב המשחק.**

---

## 2. רכיבי המערכת

| רכיב | אחריות |
|---|---|
| **API Gateway** | פעולות שאינן בזמן אמת: login, יצירת/רשימת rooms, היסטוריית משחקים. |
| **WS Gateway** | חיבורי WebSocket חיים מול הלקוחות. Async I/O, ללא thread-per-client. מעביר פקודות שחקן ל-room הנכון דרך ה-event bus, ומחזיר עדכוני state ללקוחות. |
| **Matchmaker** | מחבר/מקבץ שחקנים למשחק לפי חוקי התור. |
| **Game Allocator** | מחליט על איזה Game Server Shard ירוץ כל room, ושומר את המיפוי. |
| **Game Server Shards** | כל shard מריץ הרבה rooms במקביל, כל אחד עם GameEngine authoritative משלו ותור פקודות (command queue) נפרד ל-room. |
| **NATS Event Bus** | pub/sub פנימי בין השירותים. אירועים מתפרסמים/נרשמים **לפי room** (למשל subject בסגנון `game.room.<room_id>.*`), כך ש-shard מקבל רק אירועים של rooms ששייכים לו בפועל. זה בדיוק הפתרון לבאג ה"דליפה בין חדרים" שמצאנו במימוש הקיים (event bus גלובלי אחד משותף + broadcast גלובלי בלי תיוג room_id). |
| **Redis** | מידע זמני ומהיר: sessions, רישום rooms פעילים (`room_id → shard_id`), reconnect tokens, matchmaking queue. |
| **PostgreSQL** | מידע קבוע: users, משחקים שהסתיימו, results, move history (נכתב באופן אסינכרוני/באצוות, לא per-move). |
| **Observability** | logs, metrics, health checks, alerting, load testing. |
| **Kubernetes / K3s (+ Agones אופציונלי)** | ניהול containers, autoscaling ל-shards, ניהול fleet. |

---

## 3. מענה לשאלות החשיבה

### 3.1 — 100 מיליון משתמשים רשומים: האם SQLite מתאים?

**לא.** SQLite הוא DB של single-writer, מבוסס קובץ יחיד, מכונה יחידה. אין לו replication מובנה, אין תמיכה בכתיבה בו-זמנית ממספר שירותים ברשת, ואין scalability אופקי. ל-100M משתמשים צריך DB יחסי אמיתי — **PostgreSQL** — עם:
- Connection pooling (למשל PgBouncer) לטיפול בהרבה חיבורים מה-shards/שירותים.
- אינדוקס נכון על שאילתות users/games.
- Read replicas לשאילתות קריאה כבדות (היסטוריה, פרופילים).
- אופציה עתידית (אם נצטרך לגדול מעבר לזה): sharding עם Citus, או DB מבוזר (למשל CockroachDB). לא נדרש בקנה המידה הנוכחי, אבל כדאי לדעת כשלב הבא.

### 3.2 — 10 מיליון שחקנים בו-זמנית: האם שרת אחד מספיק?

**לא — נדרש sharding לפי room.**

- כשroom נוצר, ה-**Game Allocator** בוחר shard (למשל round-robin או least-loaded) ושומר את המיפוי `room_id → shard_id` ב-Redis.
- חיבור ה-WebSocket של שחקן יכול "לנחות" על **כל** מופע של WS Gateway (מאחורי load balancer) — ה-gateway לא צריך לארח פיזית את ה-room. במקום זאת, ה-gateway מעביר את הפקודה של השחקן דרך NATS ל-subject ספציפי ל-room (`game.room.<room_id>.move`), ורק ה-shard שבאמת מחזיק את ה-room הזה רשום (subscribed) אליו.
- כך בדיוק "כולם יכולים לשחק עם כולם, וכל אחד יכול להיכנס לכל room" — הניתוב קורה ברמת ה-event bus (לפי room_id), לא ברמת חיבור ה-TCP.
- כל shard מחזיק תת-קבוצה של rooms; shards מתווספים/מוסרים באופן אלסטי לפי העומס.

### 3.3 — נפח תעבורת רשת: מהלך כל 2 שניות לכל שחקן פעיל

חישוב גס:
- 10,000,000 שחקנים / 2 שניות ≈ **5,000,000 הודעות נכנסות לשנייה**.
- גם בגודל הודעה קטן (~200–300 בייט JSON), מדובר בכ-**1.2–1.5GB/s נכנס ≈ 10–12 Gbps**.
- בתוספת שידור-חוזר ליריבים/צופים (בערך הכפלה), התעבורה הכוללת נעה סביב **20–40 Gbps** ומיליוני הודעות בשנייה.

זה נפח **גדול מאוד** — הרבה מעבר למה שמכונה בודדת (או אפילו כרטיס רשת בודד) יכולה לטפל בו ביעילות, גם בגלל רוחב הפס וגם בגלל overhead-per-message. זו הסיבה המרכזית שהארכיטקטורה חייבת להיות מבוזרת **אופקית** על הרבה shards/מכונות, ולא להסתמך על שרת "חזק יותר" בודד.

### 3.4 — משחקים נמשכים 30–90 שניות: מה זה אומר על תפקידי ה-shards/containers?

- אורך חיים קצר מאוד למשחק אומר ש**הרבה rooms נוצרים ונהרסים בכל שנייה**. הפעלת container ייעודי לכל room תהיה איטית ובזבזנית מדי (overhead של הפעלה/סגירה מול משחק שנמשך 30–90 שניות בלבד).
- במקום זאת, כל **Game Server Shard הוא תהליך ארוך-טווח שמנהל אלפי rooms קלים בזיכרון, במקביל**, כל אחד עם תור פקודות משלו — לא container אחד למשחק.
- ה-**Game Allocator חייב להיות מהיר מאוד** (חלקיקי שנייה), כי אחרת overhead ההקצאה תופס נתח משמעותי מזמן משחק של 30–90 שניות.
- כתיבה ל-PostgreSQL (move history, results) **לא יכולה להיות סינכרונית לכל מהלך** — צריך buffering/כתיבה באצוות (למשל קודם ל-Redis, ואז flush אסינכרוני ל-Postgres), אחרת ה-DB לא יעמוד בקצב הכתיבה.
- Autoscaling (K3s / Agones) צריך להיות אלסטי ומהיר-תגובה, כי העומס נע לאורך היום בין אזורי זמן שונים בעולם.

---

## 4. מה זה מתקן מהמימוש הקיים שלנו

במימוש הקיים שלנו היה event bus גלובלי אחד המשותף לכל ה-rooms, כאשר broadcasts נשלחו **לכל** הלקוחות המחוברים ללא קשר ל-room, וללא שדה `room_id` ב-payload של האירועים — מה שגרם למהלכים ב-room אחד להשפיע גם על room אחר. העיצוב שלמעלה פותר את זה בקנה מידה גדול על ידי:
- תיוג כל אירוע ב-`room_id`.
- הגבלת ה-subject/subscription ב-NATS לרמת ה-room, כך ש-shard (והלקוחות המחוברים אליו) מקבלים אך ורק אירועים של rooms ששייכים לו.
- הימנעות מוחלטת מהסתמכות על event bus גלובלי אחד בתוך תהליך יחיד המשותף למשחקים בלתי-תלויים.

---

## 5. תוכנית יישום (Rollout)

1. לממש גרסה קטנה שעובדת מקומית עם **Docker Compose**: API Gateway, WS Gateway, Game Server Shard אחד, Redis, Postgres, NATS.
2. לוודא בידוד מלא בין rooms (שני rooms פתוחים בו-זמנית, בלי דליפה ביניהם).
3. להוסיף Matchmaker + Game Allocator רק אחרי שהזרימה עם shard בודד עובדת נכון.
4. רק אחר כך: לעבור ל-Kubernetes/K3s לתזמור multi-shard ו-autoscaling.
5. להוסיף observability (logs/metrics/health checks) לאורך כל שלב, לא בסוף כתוספת.

**סדר עדיפויות: מערכת קטנה שעובדת עדיפה על מערכת גדולה ושאפתנית שלא עובדת.**
